extends layout

block content
    h1(class='title')= title

    if genreList.length
        ul
            each genre in genreList
                li
                    a(href=genre.url) #{genre.name}
    else
     p There is no genre.
